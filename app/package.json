{
  "name": "motoman",
  "version": "0.2.0",
  "description": "Control app for Microfabricator T",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "postcss styles.css -o src/styles.css",
    "dev": "npm run build && live-server src --port=8080"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.2.5",
    "live-server": "^1.2.1",
    "npm": "^7.9.0",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.1.1"
  }
}
